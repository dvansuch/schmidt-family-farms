= form_for @farm_customer do |f|
  - if @farm_customer.errors.any?
    #error_explanation
      %h2= "#{pluralize(@farm_customer.errors.count, "error")} prohibited this farm_customer from being saved:"
      %ul
        - @farm_customer.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
